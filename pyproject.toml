[tool.pytest.ini_options]
testpaths = ["scripts"]
addopts = "--tb=short"

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "S"]

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["S101"]
"scripts/test_*.py" = ["S101"]
